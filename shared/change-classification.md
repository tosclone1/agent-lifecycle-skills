# Change Classification
REUSE: use existing behavior as-is.
MODIFY: change an existing component while preserving its role.
ADD: add a new component/capability without replacing validated behavior.
REPLACE: remove/supersede existing implementation; requires explicit justification.
